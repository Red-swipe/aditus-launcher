# app/config/__init__.py
"""Config manager package.
Provides :class:`ConfigManager` for reading, writing and updating the
`aditus_config.json` file safely.
"""

from .config_manager import ConfigManager
